# Change Log

## Workspace Normalization
- added `docs/`, `tests/`, `scripts/`, `infra/`, `logs/`
- moved vector store from `chroma_db/` to `data/runtime/vector_store/chroma_db/`
- updated code references to new vector store path
- updated `.gitignore` to match runtime storage layout
- updated README path reference
